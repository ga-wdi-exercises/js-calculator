// Create an object
let calculator = {
  // Addition
  add: function(a, b) {
    return a + b;
  },
  // Subtraction
  sub: function(a, b) {
    return a - b;
  },
  // Multiplication
  multi: function(a, b) {
    return a * b;
  },
  // Division
  divi: function(a, b) {
    return a / b;
  },
  // Exponents (Hint: Look up Javascript's Math object)
  expo: function(a, b) {
    return Math.pow(a, b);
  },
  // An operation of your choice!

};
