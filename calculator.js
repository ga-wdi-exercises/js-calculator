var calculator = {
  add: function (x, y) {
    console.log(x + y);
  },
  subtract: function (a, b) {
    console.log(a - b);
  },
  multiply: function (c, d) {
    console.log(c * d);
  },
  divide: function (e, f) {
    console.log(e / f);
  },
  exponent: function (g, h) {
    console.log(Math.pow(g, h));
  },
  remainder: function (i, j) {
    console.log(i % j);
  }
}
