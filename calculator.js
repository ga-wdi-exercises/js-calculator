var calculate = {
  //addition
   add: function(num1, num2) {
     return (num1 + num2);
   },
  //subtract
  subtract: function (num1, num2) {
    return (num1 - num2);
  },
  //multiply
  multiply: function (num1, num2) {
    return (num1 * num2);
  },
  //divide
  divide: function (num1, num2) {
    return (num1 / num2);
  }
  //exponent
  exponentiate: function (number, exponent) {
    return Math.pow(numer, exponent);
  }
 };
